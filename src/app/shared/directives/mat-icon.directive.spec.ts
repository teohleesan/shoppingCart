import { MatIconDirective } from './mat-icon.directive';

describe('MatIconDirective', () => {
  it('should create an instance', () => {
    const directive = new MatIconDirective();
    expect(directive).toBeTruthy();
  });
});
